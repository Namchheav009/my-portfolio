services:
  - type: web
    runtime: static
    name: ron-namchheav-portfolio
    buildCommand: npm install && npm run render-build
    staticPublishPath: ./
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
